name: Linter CI

on: [push]
     
jobs: 
  build:   
    runs-on: ubuntu-latest

    steps:
    - name: Linter test
      run: git clone https://github.com/SaiWeb5/python-project-lvl1.git
    - name: Pip install
      run: |
        pip install poetry
        make install      
      
               

      
      
