name: Linter CI

on: [push]
     
jobs: 
  build:   
    runs-on: ubuntu-latest

    steps:
      - name: Install poetry
        run: | 
          pip install poetry
          make install
      - name: Run flake8 
        run: make lint
               

      
      
